# Contributing

Thank you for your interest! This project is part of a professional FinTech data science portfolio.

## How to Contribute

1. **Fork** the repository
2. **Create a branch**: `git checkout -b feat/your-feature`
3. **Commit** with conventional commits: `git commit -m "feat: add SHAP waterfall plot"`
4. **Push**: `git push origin feat/your-feature`
5. **Open a Pull Request** against `main`

## Commit Convention

| Prefix | Use for |
|--------|---------|
| `feat:` | New feature or analysis |
| `fix:` | Bug fix |
| `docs:` | Documentation update |
| `refactor:` | Code restructure |
| `test:` | Tests |
| `chore:` | Maintenance |

## Code Style

- Run `black .` before committing
- Google-style docstrings on all functions
- Clear all Jupyter notebook outputs before committing
- Type hints encouraged on all function signatures

## Running Tests

```bash
pip install -r requirements.txt
pip install pytest
pytest tests/ -v
```

## Questions?

Open a GitHub Discussion or email alvenyuka2@gmail.com
