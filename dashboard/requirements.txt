# Minimal dependencies for the live dashboard only — the full requirements.txt
# at the repo root also installs training/tuning/notebook tools (Jupyter,
# Optuna, LightGBM, etc.) that the deployed dashboard never imports. Streamlit
# Cloud's free tier has a tight memory budget, so keeping this list lean
# matters for a successful cold start.
numpy<2.3
pandas>=2.0
scikit-learn>=1.6
xgboost>=2.0
joblib>=1.3
shap>=0.43
plotly>=5.17
streamlit>=1.38
